import React from 'react';
import { View, Text } from 'react-native';

export const Authorization = () => {
    return (
        <View>
            <Text>This is the Starting component</Text>
        </View>
    );
}